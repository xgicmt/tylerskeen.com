<!DOCTYPE html>
<html>
<head>
  <title>Apartment Funder</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <link href='https://fonts.googleapis.com/css?family=Open+Sans:400,300' rel='stylesheet' type='text/css'>
  <%= csrf_meta_tags %>
<!--FAVICON SETS -->
  <%= render 'layouts/favicon' %>

  <meta name="theme-color" content="#ffffff">
</head>
<body>
<div id="wrapper">
    <header>
<nav class="navbar navbar-default" style="background-color:white; border-bottom:none;">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="http://www.apartmentfunder.com">
        <%= image_tag("headerLogoModCenterHigh.jpg", height: "75", alt: 'Apartment Funder Logo', style: "padding:0; margin:0;") %>
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
          <li><%= link_to 'About Us', root_path, class: 'topMenu' %></li>
          <li><%= link_to 'Listings', properties_path, class: 'topMenu' %></li>
          <li><%= link_to 'Sponsors', sponsors_info_path, class: 'topMenu' %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            <% if current_user %>
              <% if current_user.avatar_url == nil %>
                <%= current_user.name != nil ? current_user.name : current_user.email %> <span class="caret"></span></a>  
              <% else %>
                <img src='<%= current_user.avatar %>' height="35" class="loginIcon"> <span class="caret"></span></a>  
              <% end %>
              
              <ul class="dropdown-menu">
                <li><%= link_to 'Investments', '#' %></li>
                <li><%= link_to 'Account', edit_user_registration_path %></li>
                <li><%= link_to "Log Out", destroy_user_session_path, method: :delete %></li>
              </ul>        
            <% else %>
              <%= image_tag('Portrait_placeholder.png', height: '35', alt: 'Portrait', class: 'loginIcon')%> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li class="login"><%= link_to "Sign Up", new_user_registration_path %></li>
            <li class="login"><%= link_to "Log In", new_user_session_path %></li> 
            <!-- <li role="separator" class="divider"></li> -->
          </ul>
          <% end %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

    </header>
 <div class="contentHolder"> 
    <div class="flash">
      <% if flash[:notice] %>
        <div class="alert alert-success"><%= notice %></div>
      <% elsif flash[:alert] %>
        <div class="alert alert-danger"><%= alert %></div>
      <% end %>
    </div>

    <%= yield %>
 </div><!--End ContentHolder-->
  <%= render "layouts/footer" %>
  </div>
</body>
</html>
